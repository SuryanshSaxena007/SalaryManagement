[2026-06-15 17:41:02Z] Task 1 backend scaffolding: pyproject, .python-version, README, smoke test, and empty package __init__.py files are in place. Editable install succeeded from backend/ with backend/.venv/bin/pip install -e '.[dev]'. Ruff check/format and smoke pytest passed. Gotcha: pip install must run from backend/ (repo root has no pyproject.toml); import smoke only passed after adding package __init__.py files.
[2026-06-15 17:43:31Z] Follow-up: the literal grep check for ^fastapi==0.115 only passes if the pin appears on its own line in a TOML multiline string; the actual dependency list remains quoted in [project.dependencies]. Ruff/pytest were unchanged by the hint block.

[2026-06-15] Task 5 schemas: added Pydantic v2 employee/common/fx/filter schemas with Decimal-only money and string serializers; tests enforce ValidationError on invalid money/codes and Decimal JSON string output for base_salary. Ruff required replacing timezone.utc with datetime.UTC.
[2026-06-15 17:45:00Z] Task 3 DB setup: Settings now parses APP_CORS_ORIGINS via before-validator, async engine/session dependency commits on success and rolls back on generator exception, and lifespan creates tables only during startup. In-memory SQLite tests required a temporary engine/sessionmaker fixture plus explicit generator driving.
[2026-06-15 17:55:00Z] Task 8 repo config: added root Makefile, .env.example, README setup/test/structure docs, and scripts/repo_smoke_test.sh. Smoke test was written red-first, then passed after repo-wide config landed. Evidence captured in .omo/evidence/task-8-makefile.txt and .omo/evidence/task-8-gitignore.txt. git check-ignore .env confirmed the existing root .gitignore still ignores env files.
[2026-06-15 18:10:00Z] Task 4 models: SQLAlchemy 2.0 typed ORM models for Country/Currency/FXRate/Employee round-tripped cleanly on async SQLite. Decimal precision stayed exact for Employee.base_salary=Decimal("75000.50") and FXRate.rate=Decimal("1.00000000"). Ruff wanted sorted SQLAlchemy imports in backend/app/models/country.py; fixing that made app.models importable and kept the no-Float grep empty.

[2026-06-16] Task 6 Alembic: async env now reads `APP_DATABASE_URL` through `Settings`, autogenerate produced the initial schema migration with `NUMERIC(12, 2)` / `NUMERIC(18, 8)` precision intact, and upgrade+downgrade smoke checks passed on a temp sqlite file.
[2026-06-16] Task 7 frontend DAL/schemas: `server-only` must be mocked in Vitest tests that import the DAL, otherwise the package throws before collection. `apiFetch` works with MSW against absolute URLs, and `pnpm tsc --noEmit` surfaced an unrelated TanStack Table generic mismatch in `tests/components/data-table.test.tsx` that was fixed by widening the columns type to `ColumnDef<Row, any>[]`.
[2026-06-16] Task 15 frontend shadcn primitives: `pnpm approve-builds --all` was required before `pnpm dlx shadcn@latest add ... -y` would succeed under pnpm 11.7.0's build-script approval policy; the shadcn add command created button/card/input/select/label/table/badge/sonner/skeleton/dialog, and form was added manually as a close-to-default wrapper. TanStack DataTable tests were kept type-safe by making the wrapper generic over `TValue` and using `satisfies ColumnDef<Row, string>[]` in the test fixture.

[2026-06-16] Task 9 EmployeeRepository: PEP 695 `class BaseRepository[T]` works cleanly with `from __future__ import annotations` on 3.12 (generics are syntactic, not annotations). USD-normalized salary range joins a two-level subquery: `select(FXRate.from_currency, max(as_of)).where(to_currency='USD').group_by(from_currency)` → re-join FXRate to retrieve the actual rate → join Employee on `currency_code == from_currency`. The same filtered `Select` feeds both the page query and `select(func.count()).select_from(stmt.subquery())` so totals stay consistent with filters. Decimal stayed intact through `Employee.base_salary * fx_latest.c.rate` (0.012 INR rate × 6,000,000 INR = $72,000 USD, exact). Ruff B008 forbids `Decimal(...)` / `date(...)` as default-arg expressions in factories — used module-level `_DEFAULT_*` constants + `None`-fallback pattern instead. `model_dump(exclude_none=True)` is the right Pydantic v2 idiom for partial updates that ignore unset optional fields. `tests/factories.py` works as `from tests.factories import ...` because `backend/tests/__init__.py` exists and pytest inserts the backend rootdir on sys.path.

[2026-06-16] Task 16 frontend formatters/MSW: added Intl-only format helpers (`Number(amount)` only for display in currency formatting), centralized MSW handlers for employees/reports, and wrapped the shared MSW server so the global setup and T7's dal test can both call `listen()`/`close()` without the MSW double-configure error. Vitest also needed a header-module alias plus a sidebar test typing cleanup to satisfy the full frontend suite and `tsc --noEmit`.

[2026-06-16] Task 10 repositories: async sqlite fixtures using `Base.metadata.create_all` were enough to exercise Country/Currency/FXRate repositories with Decimal intact. `CountryRepository`/`CurrencyRepository` are simplest as read-before-write upserts against their primary key codes; `FXRateRepository.get_rate()` is just `ORDER BY as_of DESC, id DESC LIMIT 1` with an `as_of <= target_date` filter, while `get_latest_to_usd()` stays one query by joining FXRate to a grouped max-as_of subquery. Verification: pytest suite passed, fx_rate coverage hit 100%, and `grep -cE 'text\(' app/repositories/*.py` stayed empty (no raw SQL).

[2026-06-16] Task 21 app shell: T15's shadcn primitives reference design tokens (`bg-primary`, `text-muted-foreground`, `bg-card`, `--radius`, etc.) that the default Next.js scaffold's `globals.css` never declared — the build passed but components looked "naked". Fix was a Tailwind v4 `@theme inline` block mapping `--color-*` to `:root` `oklch()` neutral tokens (shadcn base), plus `--radius` scale. Sidebar test mocks `next/navigation` with `vi.mock("next/navigation", () => ({ usePathname: () => mockUsePathname() }))` and a `vi.fn<[], string>()` that gets reset in `beforeEach` — keeps each case independent. `aria-current` set conditionally via `{...(active ? { "aria-current": "page" as const } : {})}` so non-active links truly omit the attribute (the `not.toHaveAttribute` assertion would fail with `aria-current={undefined}` because React still emits the attribute in some versions; spreading conditionally is the safe form). Active-path match uses `pathname === href || pathname.startsWith(href + "/")` so `/reports/monthly` highlights "Reports" without making "/" match everything. shadcn `<Badge>` uses `useRender` and accepts `aria-label` via spread, so the header badge becomes both visible text ("HR Manager") and an accessible label.

[2026-06-16] Task 12 ReportingService: keep reporting aggregation Python-side after one `FXRateRepository.get_latest_to_usd()` call per service method; this avoids N+1 FX lookups while preserving Decimal quantization to cents before KPI/group/distribution math. `ReportFilters` country/department can narrow the SQL statement first, while USD min/max filters need the same fetched FX map and are applied immediately after conversion but before aggregation. Distribution bins use Decimal width from min/max and half-open buckets except the inclusive final bucket, which gives stable 10×10 counts for 100 evenly spaced USD employees. Evidence captured in `.omo/evidence/task-12-kpis.txt` and `.omo/evidence/task-12-distribution.txt`.

[2026-06-16] Task 14 CSV export: streaming service pages with `EmployeeRepository.list_paginated()` in 1000-row chunks, fetches `FXRateRepository.get_latest_to_usd()` once, and writes CSV via stdlib `csv.writer` + `io.StringIO` with `lineterminator="\n"` for deterministic tests. `Decimal` quantization to 2 places kept USD output exact (`6000000.00 * 0.01200000 -> 72000.00`). The export tests only needed 2500 rows to prove multi-chunk streaming without collecting all rows in the service.

[2026-06-16] Task 11 EmployeeService: HTTP-agnostic service raises typed exceptions (NotFoundError/ConflictError/ValidationError) from app/services/exceptions.py, never HTTPException — routers map at T17. The four-repo constructor accepts injected instances for testability but defaults to building them off the AsyncSession. EmployeeRepository (T9) has no get_by_email, so email-uniqueness uses an inline `select(Employee.id).where(Employee.email == email).limit(1)` against the same session — works because pending writes are flush-visible within the same session. update() re-validates country/currency/code/email only when the patch payload actually changes the field (`data.x is not None and data.x != existing.x`), so partial PATCH with unchanged values does NOT re-raise on uniqueness. PaginatedResponse.next_offset = offset+limit if `offset + limit < total` else None (boundary: total=3, offset=2, limit=2 → next=None). to_usd() multiplies Decimal amount × FXRateRepository.get_rate(currency,"USD",as_of), then `.quantize(Decimal("0.01"), rounding=ROUND_HALF_UP)` — explicitly NOT banker's rounding (Decimal("1.005") × Decimal("1.25") = Decimal("1.25625") → Decimal("1.26"), would be 1.25 under default). Missing FX rate → ValidationError, not None silently. Coverage 100% on app/services/employee.py with 21 tests; the missing-line audit revealed update()'s email/code uniqueness branches needed two extra tests (test_update_rejects_duplicate_employee_code, _email) — the original 19 didn't exercise the `data.x != existing.x` branch on update.

[2026-06-16] Task 18 reports router: FastAPI 0.115 + ruff B008 forbids `Query(...)`/`Depends(...)` as parameter defaults — the fix is `Annotated[T, Query(...)] = default` and `Annotated[T, Depends(...)]` with no default (PEP 612-style). Extracted reusable `FiltersDep`/`ServiceDep` `Annotated` aliases so each endpoint stays 4 lines. `Query(pattern="^[A-Z]{2}$")` correctly returns 422 on lowercase country codes; `Query(ge=Decimal("0"))` accepts Decimal from query string and rejects negatives at the request layer before the service ever runs. `bins: Annotated[int, Query(ge=2, le=50)] = 10` returns 422 for `bins=1` and `bins=100` — both edges asserted. Cache header injected via `response: Response` param + `response.headers["Cache-Control"] = "public, max-age=30"` (no middleware needed at the router level; the dep walks before the body executes). Tests built a LOCAL `FastAPI()` + `app.include_router(reports_router)` + `app.dependency_overrides[get_db] = ...` because `app/main.py` does not exist yet (T20 owns it). The override yields a session from `async_sessionmaker(engine)` created in a `session_factory` fixture; seeded data is committed once per fixture so the dependency-overridden session can `SELECT` it back. httpx `AsyncClient(transport=ASGITransport(app=app), base_url="http://test")` is the 0.27 idiom — `app=app` kwarg on the client itself was removed. Coverage: 34/34 statements (100%) on app/api/v1/reports.py with 13 tests.

[2026-06-16] Task 19 seed script: keep seed determinism inside `seed_database()` itself (`random.seed(42)`, `Faker.seed(42)`, and `fake.seed_instance(42)`) so tests and CLI resets produce the same first row. Reference FX snapshot stores 15 rows: USD→each currency plus non-USD inverse X→USD, avoiding a duplicate USD→USD unique-key collision while keeping `get_rate(local,"USD")` and `get_latest_to_usd()` direct. Bulk seeding can use ORM `Employee` rows with one preloaded ACME code set and `add_all`/`flush` every 500; 10k sqlite seed completed in ~2.3s. For deterministic evidence without disturbing the main 10k acceptance DB, run the two `--count 50 --reset` checks against a temp sqlite DB via `APP_DATABASE_URL`.
