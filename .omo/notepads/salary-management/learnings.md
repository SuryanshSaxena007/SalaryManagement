[2026-06-15 17:41:02Z] Task 1 backend scaffolding: pyproject, .python-version, README, smoke test, and empty package __init__.py files are in place. Editable install succeeded from backend/ with backend/.venv/bin/pip install -e '.[dev]'. Ruff check/format and smoke pytest passed. Gotcha: pip install must run from backend/ (repo root has no pyproject.toml); import smoke only passed after adding package __init__.py files.
[2026-06-15 17:43:31Z] Follow-up: the literal grep check for ^fastapi==0.115 only passes if the pin appears on its own line in a TOML multiline string; the actual dependency list remains quoted in [project.dependencies]. Ruff/pytest were unchanged by the hint block.

[2026-06-15] Task 5 schemas: added Pydantic v2 employee/common/fx/filter schemas with Decimal-only money and string serializers; tests enforce ValidationError on invalid money/codes and Decimal JSON string output for base_salary. Ruff required replacing timezone.utc with datetime.UTC.
[2026-06-15 17:45:00Z] Task 3 DB setup: Settings now parses APP_CORS_ORIGINS via before-validator, async engine/session dependency commits on success and rolls back on generator exception, and lifespan creates tables only during startup. In-memory SQLite tests required a temporary engine/sessionmaker fixture plus explicit generator driving.
[2026-06-15 17:55:00Z] Task 8 repo config: added root Makefile, .env.example, README setup/test/structure docs, and scripts/repo_smoke_test.sh. Smoke test was written red-first, then passed after repo-wide config landed. Evidence captured in .omo/evidence/task-8-makefile.txt and .omo/evidence/task-8-gitignore.txt. git check-ignore .env confirmed the existing root .gitignore still ignores env files.
[2026-06-15 18:10:00Z] Task 4 models: SQLAlchemy 2.0 typed ORM models for Country/Currency/FXRate/Employee round-tripped cleanly on async SQLite. Decimal precision stayed exact for Employee.base_salary=Decimal("75000.50") and FXRate.rate=Decimal("1.00000000"). Ruff wanted sorted SQLAlchemy imports in backend/app/models/country.py; fixing that made app.models importable and kept the no-Float grep empty.

[2026-06-15] Task 2 frontend scaffolding: create-next-app@latest now installs Next 16, so pin via `pnpm create next-app@15` to satisfy `next ^15`. Use `--no-src-dir` (not `--src-dir=false`) and `--skip-install` to avoid the `[ERR_PNPM_IGNORED_BUILDS]` abort during scaffold, then `pnpm install` manually. Pitfall: `@vitejs/plugin-react@latest` resolves to v6 (Vite 7 peer); vitest@2 ships Vite 5, so pin `@vitejs/plugin-react@^4` for compatibility. pnpm 11.7.0 errors with exit 1 on ignored build scripts even when `onlyBuiltDependencies` lists them in `pnpm-workspace.yaml`; the only reliable scaffold-side workaround is `verifyDepsBeforeRun: false` in the same file (`.npmrc verify-deps-before-run=false` is silently ignored). Vitest also tries to load `postcss.config.mjs` (Tailwind v4's `@tailwindcss/postcss` chokes in CJS), so set `css.postcss: { plugins: [] }` in `vitest.config.ts` to short-circuit. For TS to see vitest globals (`describe/it/expect`) without using restrictive `compilerOptions.types`, drop a one-liner `frontend/vitest.d.ts` with `/// <reference types="vitest/globals" />`. shadcn v4 init writes a placeholder `pnpm-workspace.yaml` with `allowBuilds: ... set this to true or false` — strip it; the real key is `onlyBuiltDependencies` (kebab-case in `.npmrc`, camelCase in YAML). RED→GREEN locked the env-var contract: vitest `test.env: { NEXT_PUBLIC_API_URL }` flips the smoke test from `getByText('http://localhost:8000')` failing on rendered `'api'` to passing.
